---
import { motion } from 'framer-motion';

import Btn from '@components/Btn.astro';
import Heading from '@components/Heading.astro';

import Layout from '@layouts/Layout.astro';

import '../base.css';

const animations = {
  initial: { opacity: 0 },
  animate: { opacity: 1 },
  exit: { opacity: 0 },
};
---

<Layout>
  <motion.div
    className="flex flex-col gap-20"
    variants={animations}
    initial="initial"
    animate="animate"
    exit="exit"
    client:load
  >
    <div class="flex flex-col gap-7">
      <div class="flex flex-col gap-7 font-bold text-6xl">
        <Heading>Hello!👋🏻</Heading>
        <Heading>This is Muhesh</Heading>
        <Heading>I'm a Computer Science Student</Heading>
      </div>
      <p class="text-xl">I like to solve real world problems with software</p>
    </div>
    <div>
      <Btn
        btnText="Say Hello"
        btnRedirectLink="https://www.linkedin.com/in/muhesh-kumar-52b2391b9/"
      />
    </div>
  </motion.div>
</Layout>
